/* Create middlewares for signing up & logging in - Backend Logic*/
//! This might be deleted later, as password hashing will take place
//! in the user.js controller, where createUser is created, they are 
//! meant to both be done in the same file/function

async function registerUser() {
    try {
        
    } catch (error) {

    }
}